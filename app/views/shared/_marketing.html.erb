<div class="container">

  <div class="marketing">

    <hr class="soften">

    <% if !@recent_courses.empty? %>
      <div class="courses">
        <% @recent_courses.each do |course| %>
          <div class="course">
            <%= render "courses/course", :course => course %>
          </div>
        <% end %>
      </div>
    <% else %>
      <p>
      No Courses found
      </p>
    <% end %>
  </div>

</div>

