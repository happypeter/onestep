<%= form_for(@course, :url => "/course") do |f| %>
  <%= f.hidden_field :user_id %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label t('course_name') %>
    <br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label t('course_title') %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label t('course_poster_url') %><br />
    <%= f.text_field :poster_url %>
  </div>
  <div class="field">
    <%= f.label t('cat') %><br />
    <%= f.text_field :cat %>
  </div>
  <div class="field">
    <%= f.label t('course_description') %><br />
    <%= f.text_area :description %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
