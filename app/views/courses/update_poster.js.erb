$('img#poster').replaceWith('<%= image_tag @course.poster_url,:alt => "Poster Image", :id => "poster"%>');
$.colorbox.close();
$('.modal-footer form').find("input[type='submit']").prop('disabled',false);