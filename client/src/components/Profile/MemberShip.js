import React from 'react'
import PropTypes from 'prop-types'

const MemberShip = ({ isMember }) => <div>MemberShip</div>

MemberShip.propTypes = {
  isMember: PropTypes.bool.isRequired
}

export default MemberShip
